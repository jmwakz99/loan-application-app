schema:
  - "http://localhost:5001/graphql"
documents:
  - "api/graphql/**/*.graphql"
generates:
  types/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
